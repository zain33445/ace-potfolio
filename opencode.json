{
  "$schema": "https://opencode.ai/config.json",

  "mcp": {
    "motion": {
      "type": "remote",
      "url": "https://mcp.motion.so/mcp",
      "enabled": true,
      "headers": {
        "Authorization": "Bearer {file:.motion-token}"
      }
    }
  }
}
